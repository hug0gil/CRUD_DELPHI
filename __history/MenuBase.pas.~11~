unit MenuBase;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants,
  System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Data.DB, Vcl.ExtCtrls, Vcl.Buttons,
  Vcl.DBCtrls, Vcl.Grids, Vcl.DBGrids, Vcl.StdCtrls;

type
  TFormMenuBase = class(TForm)
    ButtonVolver: TButton;
    DBGrid: TDBGrid;
    DBNavigator: TDBNavigator;
    Panel1: TPanel;
    ButtonActualizar: TButton;
    ButtonEliminar: TButton;
    ButtonAgregar: TButton;
    Shape1: TShape;
    procedure ButtonActualizarClick(Sender: TObject);
    procedure ButtonEliminarClick(Sender: TObject);
    procedure ButtonAgregarClick(Sender: TObject);
    procedure FormCreate(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FormMenuBase: TFormMenuBase;

implementation

{$R *.dfm}

procedure TFormMenuBase.ButtonActualizarClick(Sender: TObject);
begin
  // Actualizar
end;

procedure TFormMenuBase.ButtonAgregarClick(Sender: TObject);
begin
  //Añadir
end;

procedure TFormMenuBase.ButtonEliminarClick(Sender: TObject);
begin
  // Eliminar
end;

procedure TFormMenuBase.FormCreate(Sender: TObject);
begin
//OnCreate
end;

end.
